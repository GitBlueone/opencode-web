# OpenCode Web 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置值

# ==================== 服务器配置 ====================

# Web 服务器端口（默认：3000）
WEB_SERVER_PORT=3000

# OpenCode serve 主机地址（默认：localhost）
OPENCODE_HOST=localhost

# ==================== OpenCode 配置 ====================

# OpenCode serve 默认端口（默认：4096）
DEFAULT_OPENCODE_SERVE_PORT=4096

# OpenCode session 存储目录（默认：系统默认路径）
# Windows: C:\Users\YourName\.local\share\opencode\storage\session
# Linux/macOS: /home/yourname/.local/share/opencode/storage/session
OPENCODE_STORAGE_DIR=

# 最大并发 serve 实例数（默认：3）
MAX_CONCURRENT_SERVES=3

# ==================== 超时配置（毫秒） ====================

# 端口检查超时（默认：3000ms）
PORT_CHECK_TIMEOUT=3000

# 等待端口就绪超时（默认：30000ms）
WAIT_FOR_PORT_TIMEOUT=30000

# OpenCode 请求默认超时（默认：30000ms）
OPENCODE_REQUEST_TIMEOUT=30000

# 发送消息超时（默认：60000ms）
SEND_MESSAGE_TIMEOUT=60000

# 健康检查间隔（默认：60000ms）
HEALTH_CHECK_INTERVAL=60000

# ==================== 前端配置 ====================

# API 基础路径（默认：/api/sessions）
API_BASE=/api/sessions

# SSE 重连延迟（默认：3000ms）
SSE_RECONNECT_DELAY=3000

# 默认工作目录（默认：系统用户目录）
# Windows: C:\Users\YourName
# Linux/macOS: /home/yourname
DEFAULT_DIRECTORY=
